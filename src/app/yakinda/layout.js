export const metadata = {
  title: 'KitapLab - Yakında',
  description: 'Kitap tutkunları için özel bir deneyim hazırlıyoruz',
};

export default function YakindaLayout({ children }) {
  return children;
}